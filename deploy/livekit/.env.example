# LiveKit Deployment Environment
# Copy to .env and fill in values

# Domain for this LiveKit node (must have DNS pointing to this server)
DOMAIN=livekit-use.nyptidindustries.com

# API credentials (generate with: docker run --rm livekit/generate generate-keys)
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
